Infiproxy `0.1.0-beta.1`. Документация хранится вместе с исходным кодом и
публикуется из `wiki/`. При расхождении проверяйте установленный Git commit,
локальный конфиг и документацию конкретной версии runtime.
